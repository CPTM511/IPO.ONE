# settlement

Owns provider spend settlement records and state changes. It records whether a prepared payment was settled or failed, and emits events for audit.

Public interface: `SettlementService`.
