# spend-policy

Owns Provider allowlisting, spend policies, and spend request approval/rejection. It never allows arbitrary recipients and does not move funds.

Public interface: `SpendPolicyService`.
