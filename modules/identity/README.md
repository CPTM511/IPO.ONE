# identity

Owns Subject, Principal, and AccountBinding lifecycle. It keeps Agent, Human, Org, Originator, Principal, and WalletAccount as separate concepts and blocks production Human lending paths in the MVP foundation.

Public interface: `IdentityService`.
