# admin

Owns Admin/Risk visibility, audit timelines, exposure aggregation, and admin actions through public service interfaces. It does not bypass module service methods.

Public interface: `AdminService`.
