# WEB-026 — Task-first IPO.ONE product experience

Status: Founder-authorized local implementation, 2026-09-05. Parent program, not an all-at-once implementation issue. WEB-026A–D presentation is implemented; WEB-026E records browser evidence and its actual-runtime blocker. WEB-026F is a proposed separately reviewed increment.

## Objective and authority

Implement `docs/design/IPO_ONE_PRODUCT_EXPERIENCE_ALIGNMENT_v1.0.md`: current task and meaningful account summary first; every allowed capability discoverable; complete Human and Principal/Agent paths; polished fintech object presentation and recoverable interactions.

Read current AGENTS.md, Product Constitution v1.6, Charter v1.1, Optimization Measure and Product Engineering and Experience Standard. They supersede historical task assumptions. Use the existing shared kernel, protocol, authentication, policy and persistence. Borrower/Controller navigation changes cannot grant capabilities.

Functional baseline: `9636ec2` (current workspace product and Whitepaper/public-tail changes). Approved visual source: `d94b78e` (WEB-012B). Carry forward presentation deliberately, without copying older backend, migrations, policies, generated Whitepaper content or acceptance claims. Keep the old 4186/4187 review available.

## Delivery program

Execute the child issues in order. Only one is active for application edits at a time. Record changes, tests and remaining runtime prerequisites before proceeding; a blocked external verification must not prevent independent authorized presentation/regression work in subsequent issues. Do not mislabel a blocked issue complete.

| Issue | Outcome |
| --- | --- |
| WEB-026A | Baseline reconciliation and shared appearance; restore approved homepage on the candidate's ordinary entry while retaining current product/Whitepaper |
| WEB-026B | Real authentication availability, legible access dialog, task-first role navigation, session/return recovery and shared app shell |
| WEB-026C | Human current task, compact account summary, exact terms and repayment; Principal/Agent setup, task and receipt presentation |
| WEB-026D | Capital/Risk/remaining allowed views, shared responsive components, progressive detail and full navigation preservation |
| WEB-026E | Browser capability, negative/recovery, accessibility, theme and viewport acceptance; actual local service integration and Founder review |

## Experience contracts

- Default product entry is the selected design, not a secret review query. Keep legal deep links, refresh, history and the existing authorized post-login intent.
- Use current task / next action / account summary / recent result. Every primary surface has one dominant action. Remove duplicated promotional headers and repeated confirmation controls from the primary reading order; keep their existing legitimate capability reachable.
- Borrower primary navigation: Next, My credit, Activity, Settings. Controller: Tasks, Agents, Activity, Settings. Capital and Risk retain their real role-specific surface and contextual tasks. All other allowed views remain under visible More tools; unavailable views retain truthful state. No new Provider role or unsupported route.
- One session control and one compact environment disclosure. Show current role/account without implying a workspace toggle authenticates another identity. Keep actual authentication methods derived from host and browser availability.
- Forms preserve input and context through pending/error; disabled controls explain why. Confirmation labels name economic/authority effects. Show server-confirmed results persistently; never drive finance or authority from animation.
- Reuse server-derived Human/Agent next-action models. Read/prepare may be automatic within existing policy. Protected mutations remain explicit and exact. Agent machine execution and receipts remain actual authorized API/MCP facts.
- Preserve all existing role access, M2/M3 availability, current-user Evidence and reporting rules. Do not replace a missing integration with a success fixture.
- Keep the approved graphite/blue direction, expressive Offer/authority/progress objects, precise money typography, and light/dark parity. Motion must respond immediately and yield to input. No new dependency or framework migration.

## Runtime and authorization

Existing local durable services were observed on 8895–8898 with `local_no_funds`, wallet authentication enabled and no OIDC providers. Browser authentication is not yet verified. Reuse approved authentication; do not add Google/email providers, forge sessions, bypass wallets, change Origin/CSRF protections or broaden roles.

Local candidate processes, existing approved local runtime composition, synthetic test hosts and test data may be used. Distinguish them in UI/evidence. A reversible local UI-only runtime refresh may use the existing stack after preserving source/runtime identity and rollback; no database reset, new secrets, activation, chain write, new contract or cloud deployment. If actual authentication requires the Founder, finish the concrete review candidate first, then request that specific user interaction.

## Required evidence and rejection criteria

- Inventory all existing allowed role views, visible entry, action/result and verification boundary. Do not call a source marker test product acceptance.
- Run existing browser journeys plus targeted regression for real defects: login recovery contrast, readable middle-width summaries, one primary task, every allowed advanced destination, back/reload/sign-out, fast duplicate action, expiry/unavailable and reduced motion.
- Inspect 1440, 1058, 1024, 768, 390, 320 widths as appropriate; use light/dark, keyboard/focus, 200% zoom. No fragmented amount/word columns, clipped dialogs or invisible focus.
- Run web unit and presentation tests, lint, web bundle integrity, current Whitepaper generation checks, transport/security checks relevant to static delivery, and full browser regression. Keep failures attributable and do not alter assertions merely to hide regressions.
- Actual-service acceptance uses real authenticated operations and durable data, including refresh/re-login recovery. Tests and fixture hosts are supporting evidence only. No automatic wallet signature or external economic action.
- Hand off a live clickable candidate and maintain the local process for review. Document restart/stop/rollback, source SHA, backend identity and evidence timestamp.
- Track CODE / RUNTIME / DEPLOYED / REACHABLE / VERIFIED separately. Final product verdict is BLOCKED — NOT COMPLETE if deployed or authenticated evidence remains missing. PASS — DEPLOYED AND USER-VERIFIED requires separate authorization and deployed-SHA acceptance.

## Engineering and rollback

Keep focused presentation modules below 500 lines where practical. Avoid new domain logic in app.js; explicit render hooks are preferable to observing the whole DOM. Preserve handlers when moving controls. Scope files per issue, document intentional markup expectation updates, and keep current source authority clear.

No schema/migration impact is expected. Each increment can be reverted by commit; local runtime rollback restores the prior exact UI/runtime image without deleting PostgreSQL data. No push, merge, cloud deployment, or modification of other worktrees' runtime while changes are unverified.

## Actual runtime finding

Do not execute the initially considered UI overlay/restart: the existing process predates mounted backend changes and its database is missing 0070–0073. Current read-only evidence and the concrete follow-up scope are in WEB-026F. UI/browser readiness does not establish real-account or durable-service completion.
