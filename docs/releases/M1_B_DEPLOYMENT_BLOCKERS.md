# M1-B Deployment Blockers

## Current status

Status: `EXACT_ACCEPTANCE_PENDING_DEPLOYMENT_NOT_AUTHORIZED`

The Founder Deployment Architecture Amendment superseded the earlier unresolved
Railway/GCP provider decision, but current M1-B closure does not authorize a
deployment. If separately authorized after exact acceptance, the M1-B L1
surface is the Primary Vercel project with Node.js Functions, Primary-only Cron,
and one Neon PostgreSQL 17 database. A hosted Risk project is excluded until a
separately approved phishing-resistant authentication topology is composed for
M1-C/L2.

The existing Vercel team and project are:

```text
team: cptm-111-s-projects
project: ipo-one-internal
project ID: prj_KLyAPHnFCtQAxIbUjdJa012gAq7Y
risk project: ipo-one-internal-risk (historical baseline only; not an M1-B target)
```

No custom domain, paid Neon tier, paid add-on, third project, external worker,
queue, cache, signer, or production financial path is authorized.

## Remaining M1-B closure blockers

1. Cut one exact normal candidate commit that excludes protected user WIP and
   unrelated paths. This is not an RC tag, seal, merge, or deployment.
2. Run all 61 migrations through `0061_execution_account_bindings` and the
   exact repository, security, transport, PostgreSQL, container, and CI gates.
3. Complete exact-candidate Human, Principal/Agent, and Capital Partner browser
   journeys, durable receipts, negative cases, and full-stack restart recovery.
4. Prove the mandatory Risk boundary: all policy-derived recent-MFA operations
   deny under exact-source SIWE contexts; a real post-restart SIWE-only Risk
   session cannot reach an exposed protected read or mutation; protected state
   is unchanged; no weak fallback or hosted Risk surface exists.
5. Verify the private P0-5 v2 Evidence against the same clean SHA and tree. If
   deployment is not authorized, record `deployment_pending`, `releaseId:null`,
   no hosted surfaces, and no hosted browser rows.
6. Correct PR #20 metadata to the final exact candidate without merging,
   promoting, changing aliases/DNS, tagging, or sealing.

Until these items pass, `REQ-CREDIT-009`, `REQ-UX-001`, `REQ-UX-003`, and
`REQ-UX-005` remain current blockers. Full privileged `REQ-UX-004` remains
`IMPLEMENTED_UNVERIFIED` and deferred to M1-C/L2; its deferral does not remove
the M1-B Risk fail-closed security gate.

## Historical provider decision

`deploy/closed-pilot/provider-selection.pending.json` remains immutable
historical evidence for the earlier GCP/Cloud Run proposal. It is superseded
only for the M1-B deployment work package and must not be rewritten as if its
original observation had approved provisioning.

## Prohibited escalation

- RC branch or tag;
- release or production financial claim;
- paid Neon plan or Vercel add-on without Founder approval;
- fee runtime;
- signer, transfer, withdrawal, custody, or venue write;
- Human production lending or real funds;
- new chain, credit model, broader Capital Partner expansion, UI redesign,
  marketing, broad refactor, or dependency major upgrade.
